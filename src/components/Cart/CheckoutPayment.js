import classes from './CheckoutPayment.module.css';

const CheckoutPayment = () => {
  return (
    <div className={classes['checkout-payment-container']}>CheckoutPayment</div>
  );
};

export default CheckoutPayment;
